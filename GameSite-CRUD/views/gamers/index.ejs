<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Browse Gamers</title>
    <link rel="stylesheet" href="/stylesheets/gamersindex.css">
</head>
<body>
    <%- include('../partials/_hamburger.ejs') %>
    
        <div class="page-header">
            <h1>Browse All Gamers</h1>
            <p>Click on any profile to view their details</p>
        </div>
    
        <div class="users-grid">
            <% users.forEach(user=> { %>
                <a href="/gamers/<%= user._id %>" class="user-card">
                    <img src="<%= user.image %>" alt="<%= user.username %>" class="user-avatar">
                    <p class="user-name">
                        <%= user.username %>
                    </p>
                </a>
                <% }); %>
        </div>
</body>
</html>