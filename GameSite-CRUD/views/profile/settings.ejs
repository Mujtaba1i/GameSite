<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit account settings</title>
    <link rel="stylesheet" href="/stylesheets/profilesettings.css">
</head>

<body>
    <%- include('../partials/_hamburger.ejs') %>
    
    <div class="settings-container">
        <h1>Account Settings</h1>
        <p class="subtitle">Manage your profile and privacy settings</p>
        <% if (errorMessage) { %>
            <div class="error-message">
                <h3 style="color: red;"><%= errorMessage %></h3>
            </div>
        <% } %>

        <form action="/profile/settings?_method=PUT" method="POST">
            <div class="form-group">
                <label for="username">Username</label>
                <input type="text" name="username" id="username" value="<%= user.username %>" placeholder="Enter your username"/>
            </div>

            <% if (!isSignedWithGoogle) { %>
                <div class="form-group">
                    <label for="password">Password</label>
                    <input type="password" name="password" id="password" placeholder="Enter your password"/>
                </div>
            <% } %>

            <div class="form-group">
                <label for="accountState">Account Visibility</label>
                <select name="accountState" id="accountState">
                    <option value="Public" <%= user.accountState === "Public" ? "selected" : "" %>>Public</option>
                    <option value="Private" <%= user.accountState === "Private" ? "selected" : "" %>>Private</option>
                </select>
            </div>

            <button type="submit" class="btn-submit">Save Changes</button>
        </form>
        <br>
        <form action="/profile/settings?_method=DELETE" method="POST">
            <% if (!isSignedWithGoogle) { %>
                <div class="form-group">
                    <label for="delete-password">Password</label>
                    <input type="password" name="password" id="delete-password" placeholder="Enter your password"/>
                </div>
            <% } %>
            <button type="submit" class="btn-delete">Delete Account</button>
        </form>
            
        <div class="back-link">
            <a href="/profile">← Cancel and go back to profile</a>
        </div>
    </div>

</body>